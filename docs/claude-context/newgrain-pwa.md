---
name: newgrain-pwa
description: "Flagleaf installable PWA (ai.flagleaf.ru/app); tabs = 💬 Ассистент (chat+recognition merged) · 🔭 Скаутинг · ✅ Проверка"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3441be93-9176-4fd2-9061-613379401bfe
---

The agronomist web app (ai.flagleaf.ru/app) is being turned into an **installable PWA** —
chosen over native/store apps because of Russia distribution friction (App Store is hard
from RU; RuStore only covers Android) and solo-founder upkeep. Add-to-Home-Screen works on
both iPhone (Safari) and Android (Chrome), no store, no VPN. See [[newgrain-web-email-login]]
(the login it sits behind) and [[newgrain-web-ai]] (the public chat it reuses).

**Roadmap (ALL DONE):** A installable ✅ · B one app/two tabs ✅ · C offline capture ✅ · D push ✅.

**Phase D (web push):** `bot/push.py` (pywebpush + VAPID) `send_push(tg_user_id,title,body,url)` fans to
all the user's devices, prunes dead endpoints (404/410). `push_subscriptions` table (0033, keyed by
tg_user_id). VAPID keys: public in `.env` (served via `GET /api/push/key`), **private in Lockbox +
.env as `VAPID_PRIVATE_KEY`** (base64url raw 32-byte; pywebpush `Vapid01.from_string` parses it).
API: `/api/push/subscribe`, `/api/push/test`. SW has `push` + `notificationclick`. App shows a
«🔔 включить уведомления» banner (only in installed PWA where Notification/PushManager exist — iOS
16.4+ standalone only) → `subscribePush`; `ensurePushIfGranted` refreshes the sub on each open.
Trigger wired: chief-agronomist review approve/correct (bot `on_review`/`on_review_edit`) pushes the
submitter. More triggers (reminders, new-pending-for-chief) are easy follow-ons.

**Phase C (offline capture):** `web/app/index.html` queues submissions in IndexedDB (`flagleaf-q`,
store `q`; photos stored as File blobs) when `navigator.onLine` is false OR the POST fails with no
status / 5xx. A gold «Ожидают отправки» panel lists pending items; `flushQueue()` auto-sends on the
`online` event, on app entry (enterApp), and via «отправить сейчас». 4xx → dropped (won't loop);
401/403 or transient → kept. NO background sync (iOS unsupported) — flush is foreground-only.

**Built (Jun 2026):**
- `web/app/manifest.json` (standalone, gold theme, icons 192/512/maskable in web/ai/, served at root).
- `web/app/sw.js` — offline app-shell cache; `/api/*` always live; navigations network-first then
  cached tab. **Bump `CACHE` (flagleaf-shell-vN) on every shell change** or installed apps keep the old one.
- `web/app/assistant.html` — in-app AI assistant = a COPY of the public `web/ai/index.html` + manifest +
  SW + the shared header tab bar. ⚠️ Two copies now: edit BOTH when changing the assistant (unify later).
- Both pages: two-row sticky `<header>` = logo/user row (`.hrow`) + tab strip (`.tabs`:
  💬 Ассистент `/app/assistant.html` · 📷 Загрузка `/app/`). Upload page shows tabs only after login.
- Fullscreen/iOS: `header` has `padding-top:env(safe-area-inset-top)` so it clears the notch;
  status-bar meta = `default` (dark glyphs on the white header, was `black-translucent` = invisible).
  iOS caches status-bar style at install → reinstall to pick up that change.
- In-app nav buttons (‹ back / › forward / ⟳ refresh) top-left on both pages (no browser chrome in standalone).

**Deploy:** static files copied to `/var/www/ai/app/` (chmod 644); icons to `/var/www/ai/`. nginx
`location /app/` serves them — no container rebuild for front-end-only changes (only the `api` rebuilds
for backend changes). Both pages register `/app/sw.js` with `updateViaCache:'none'`.

**«Мои загрузки» panel (1 Jul 2026, Almas's ask).** Agronomists asked to confirm their own uploads actually landed (the app only showed a transient «Загружено» toast + running counts; Telegram had /history, the app didn't). Added `GET /api/my-uploads` + `db.get_user_uploads(user_id)` (caller's own recent submissions with pipeline status + is_video). New `#myUps` panel in web/app/index.html under the upload button — pulls the list from the SERVER (proof it's not just local), plain-language status (принято / ждёт разметки / на проверке у старшего агронома / размечено / отклонено), green ✅ for any on-server status, ⚠️/🚫 for warn/bad. Refreshed on login, after each photo AND video upload, and after the offline queue flushes; ⟳ button to refresh manually. SW v21→v22, announce #19. Verified: Almas (user id 2) → his real uploads with statuses. Builds on [[newgrain-motivation-no-gamification]].

**Scouting motivation flipped to intrinsic + GPS trigger (4 Jul 2026, founder directive).** REMOVED all scouting nudges/gamification: the smart scout reminder (deleted `bot/scout_reminder.py`, its cron, and the `switch-on-scout-reminders` scheduled task), `/api/scout/status` + `scoutNudge` + `db.scouting_idle_users`/`user_scout_days_since`, the 🔭 composer button, and the «контрольные поля»/demo-field card (`loadControlFields`/`pickControlField`; `/api/demo-fields` endpoint still exists, just unused). Push banner reworded to review-only. NEW motivation = the helpful reply itself (agronomists scout because they learn something back). NEW trigger: on app open, `geoPrompt()` calls `navigator.geolocation` → **`POST /api/field-at-point {lat,lon}`** → `db.field_at_point` (PostGIS ST_Contains, ANY field, farm-scoped, no is_pilot filter) → if standing on a field, an in-chat card «📍 Вы на поле X. Что здесь происходит? пришлите видео» with 🎥 (new `#camvid` capture=video input) / 📷 buttons, and the field is auto-selected so the reply+record attach to it. Verified: a field centroid resolves back via field_at_point; /api/field-at-point 401 w/o auth; /api/scout/status now 404. Needs browser location permission (denied → no prompt, silent). The granular capture card (categories/40-photos) is now reachable ONLY by typing «загрузить/обследование/скаут» (SCOUT_RE) — no button; **flagged to founder** whether to keep it. Opening-content priority in boot: chief review digest > geoPrompt > camera hero. SW v45. Supersedes the scout-reminder + control-fields notes below.

**Enquiry vs scouting UNIFIED — bot decides, always helps AND learns (4 Jul 2026, founder directive).** The user no longer picks «вопрос» vs «скаутинг» for a single send. EVERY photo/video sent through the composer now BOTH (a) gets a helpful comment-aware reply AND (b) is filed as scouting for learning. The comment (question or observation, typed or voice-transcribed) only shapes the reply — `_DIAG_SYS` now detects report-vs-question and starts with «Записал: …» for observations while still advising (every observation implies «что делать?»). Impl: `runPhoto` → `learnPhoto` fires `/api/submit`(category=scouting, field_id, comment) fire-and-forget + `diagnose` for the reply (two small uploads). `runVideo` → `/api/diagnose-video` which now stores the clip server-side in ONE upload (added `_optional_user` + `field_id`/`comment` params → shared `_store_scout_video` helper, factored out of `/api/scout-video`). The recognition-CHIPS path (recognize→pick→decide) is retired for single sends — the species guess now lives in the diagnose reply text; `guessBubble`/`decideInChat`/`logToFlywheel` remain defined but unused. The deliberate 🔭 capture card (categorized bulk, 40 photos, control fields) is unchanged. Consequences to watch: enquiry photos now create 'scouting' submissions (corpus grows; dedup by hash prevents exact dupes; category=scouting → 'stored', NO chief-review flood); enquiry VIDEOS from juniors DO enter Almas's review (pending_review, as videos always have). SW v44.

**Rich in-chat capture card + composer reflow (4 Jul 2026).** Scouting is no longer a simple one-shot — tapping **🔭** (now a composer button) opens an in-chat **capture card**: category chips (обследование / сорняк / болезнь / вредитель / стресс / контроль / результат обработки — `CATEGORIES`, matches handlers.py), a species datalist for weed/disease/pest (`DISEASES`/`PESTS`), **up to 40 photos at once** (multi-select grid with per-tile remove), a 🎥 video option, and a comment → submits via `/api/submit` (photos, with chosen category+species) or `/api/scout-video` (video), offline-queued. **«Контрольные поля»** weekly nudge is back as a tappable card (`loadControlFields` ← `/api/demo-fields`): shown on open when a control field is >7 days stale, or on typing «контрольные поля»; tapping a row `pickControlField` → adds it to the field `<select>` if outside the pilot list → selects it → opens the capture card. **Composer reflow** (founder ask): ➤ send now sits beside the textarea (`.toprow`); the four buttons 📷🖼🎤🔭 are a full-width row below (`.btnrow .iconbtn{flex:1}`). The old scoutMode/scoutSubmit/scoutBar banner was removed (card-based now). SW v43. **Scout reminder still PAUSED** — a one-time Claude scheduled task `switch-on-scout-reminders` fires **2026-07-18 09:00 MSK** to remind the founder to un-pause it (founder asked to keep it off «for some time»).

**Scouting merged into the assistant → ONE-screen app + login-everywhere (4 Jul 2026).** The Скаутинг page (`index.html`) is now **login-only** (after login → `location.replace('/app/assistant.html')`; its upload UI is retired-but-kept to avoid churn). The assistant **requires login** for everyone now (no more guest/public app mode — `boot()` redirects to `/app/` without a valid session); the separate public marketing chat at `/ai/` is untouched. **No tabs** anymore — chat, recognition, video-comment, chief review AND scouting all live in the assistant. **Scouting mode** (founder chose explicit-mode over ask-after): «🔭 Заскаутить» chip (in the contribution strip) / typing «скаут…» / the reminder → requires a REAL field selected in the top bar (not «другое поле») → gold banner over the composer → the next photo OR video is filed as обследование (video→`/api/scout-video`, photo→`/api/submit` category=scouting), NOT enquiry. A normal video (no scout mode) stays enquiry (`/api/diagnose-video`). The **offline IndexedDB queue + flush listeners + push-subscribe** were ported from index.html into the assistant (critical for field signal). **Composer reflowed**: textarea on top, the 4 buttons (📷🖼🎤➤) on a row below. **«ваш вклад»** = collapsible `#contribBar` strip pinned above the composer (with the 🔭 scout CTA); **«мои загрузки»** has no panel — summon by texting «мои загрузки» (`MYUP_RE`). **Smart scout reminder**: `bot/scout_reminder.py` + `db.scouting_idle_users(days)`/`user_scout_days_since` + `GET /api/scout/status` (SCOUT_IDLE_DAYS=3) → in-chat idle nudge card on open, and a daily push+Telegram to only the idle (verified query: 2 idle — Сорока 10d, Костенников 4d, not everyone). **Cron installed PAUSED** (`#PAUSED 10 5 * * *` = 08:10 MSK) — founder must un-pause to start real nudges (kept off per the on-hold stance). SW v42. **Retired from the app** (flag if the founder wants them back): granular upload categories stress/control/treatment_result + multi-photo(40)-at-once + client GPS badge (server still reads EXIF) + «контрольные поля» weekly nudge. Supersedes the merged-assistant + separate-scouting notes below.

**Video commenting in the assistant (4 Jul 2026).** The chat now comments on short field VIDEOS, not just photos. The 🖼 attach accepts `image/*,video/*`; a video is staged like a photo and shown in-thread (`<video>`). New `POST /api/diagnose-video` (public, «diag» rate bucket, ≤60 MB): `bot/video_frames.extract_frames` (ffmpeg — in the shared Dockerfile, so the api image has it) samples 6 frames, keeps the 3 sharpest (variance-of-edges focus score, PIL, no numpy) → `bot/video_transcribe.transcribe_video` for narration → `bot.diagnose.diagnose_video`. The vision call was refactored to multi-frame: `_vision_call(frames, intro)` (old `_vision_sync` = `_vision_call([img])`); `diagnose()`/`diagnose_video()` share a new `_compose(vd, q, crop, field, extra_ctx)` (same Госкаталог+literature grounding + confidence-temper). **Key limitation, told to founder:** the in-RU qwen model reads STILLS not motion (true-video Gemini is geo-blocked/152-ФЗ), so a video = sharpest frames + narration, not motion analysis. Verified in the api container: extract_frames→3 JPEGs, transcribe→'' clean on a silent clip; full HTTP path on a REAL plant clip is the on-hold on-device test (a synthetic test-pattern makes qwen over-reason past the timeout — not representative). SW v41. Builds on [[newgrain-labeling-pipeline]] (scouting-video transcription infra) + the merged assistant below.

**Scan merged INTO the assistant (4 Jul 2026).** The standalone «🔍 Что это?» tab (`scan.html`) was folded into **💬 Ассистент** (`web/app/assistant.html`) as one chat-first screen (founder's choice, Вариант A). Tabs are now **💬 Ассистент · 🔭 Скаутинг · ✅ Проверка** (chief-only). `scan.html` is now a redirect stub → assistant.html (kept so bookmarks/SW-cache don't 404). The merged screen: field+crop bar pinned on top (from scan); a big camera hero as the empty state that collapses to a compact composer (📷 camera `capture=environment` · 🖼 gallery/file · 🎤 voice · text · ➤) once the conversation starts. **Photo with NO caption → `/api/recognize` guess chips (rendered as a bot bubble) → tap → streamed field-grounded decision (`/api/chat/stream` structured:true) + flywheel `/api/submit`.** Photo WITH a caption → `/api/diagnose` one-shot. Text → normal chat (field/plan questions answered in-chat via server-side routing — see below). Recognition/diagnose are public; only the flywheel submit needs login (fire-and-forget, guests just don't contribute). Also this session: **field questions answered in-chat** — `/api/chat` + `/api/chat/stream` now run `_field_route()` (regex `пол[еяю] №? N` + plan-intent) → `field_card_text` as grounding context, or `generate_field_plan` for «план по полю N»; the old «План по полю» button was removed. SW v38. The «Культура» dropdown role: pre-sets crop context so weed Qs need not repeat it; replaceable by naming the crop in-question or the bot asking (offered ask-and-remember, founder hasn't decided). NOT device-tested on a real phone yet. Supersedes the scan-journey note below.

**App v2 Phase 1 — the scan journey SHIPPED (2 Jul 2026).** `web/app/scan.html` (new primary tab «🔍 Определить», added across all app pages): camera-first capture → `POST /api/recognize` (NEW; wraps `diagnose._vision_sync` → structured {top, alternatives, confidence, class} for the guess card; public/IP-rate-limited like /api/diagnose) → confidence-gated guess card (≥60 «скорее всего», else «помогите определить») → tap any guess → streamed **field-grounded decision** via `/api/chat/stream` (reuse Госкаталог+ЭПВ; product+dose+timing+savings) → «Другое» text/voice (reused PCM recorder → /api/transcribe) → decision. Every choice fires `/api/submit` (photo + user's verdict as species) = the CVAT flywheel, correction=label. Field chip (/api/fields) → crop grounds recognize + decision. SW v24. **Verified on prod:** /api/recognize returns structured guesses; decision stream returns full grounded answer w/ ЭПВ. **NOT yet device-tested** (camera capture, auth'd flywheel submit, voice mic need a real logged-in phone — Almas). Realises Phase 1 of [[newgrain-app-v2-vision]] (docs/app-v2-phase1-spec.md). Announcement deliberately held until on-device UX confirmed.

---
name: newgrain-status-2026-05
description: NewGrain/Flagleaf build status, current state, and open threads (as of 2026-06-11)
metadata: 
  node_type: memory
  type: project
  originSessionId: 88a17f24-0305-4186-87de-ccb72cc8bca8
---

**Updated 2026-06-11.** (Earlier May-25 content was superseded — Whisper→Yandex, prod deployed, etc.)

**Live in production** (Yandex Cloud VM 111.88.248.159 — static; was ephemeral 158.160.46.89, `docker-compose.prod.yml`): photo flow (photo → field → category → species/disease/pest → comment → S3 + `submissions` row), real pilot fields (New Grain Co), nightly backups, commands (/history /stats /fields /help /problem /finish /cancel /all /adduser /removeuser). Bot polls via the **Cloudflare relay** (`TELEGRAM_API_BASE`) to get around the RKN Telegram block.

**Voice/text — both Yandex now** (migrated OFF faster-whisper 10 Jun): RU transcription = **Yandex SpeechKit** (`bot/transcribe.py`); RU→EN translation of voice AND typed comments = **YandexGPT** (`bot/translate_llm.py`), grounded in the species dict. Needs `YC_API_KEY`+`YC_FOLDER_ID`. `comment_voice_text(_en)`, `comment_text_en` columns. **Bug fixed:** `update_submission` allowed-set had dropped `comment_voice_text`.

**Labeling pipeline** ([[newgrain-labeling-pipeline]]): CVAT Cloud, **55 labels** (23 weeds + 11 diseases + 15 pests + 6 stresses). Categories incl. disease picker + **pest picker** (`bot/taxonomy.py`; pests from Syngenta atlas TOC, names only per LICENSING.md). Nightly cron (03:30): voice backfill → export → reference-sheet deliver → import+recycle. Slot recycling fixed to detect **job state** (not task status). Annotation **reference sheet** (`labeling/reference.py`, `--deliver` → Object Storage link). Photo **dedup** at upload (`image_hash`). Dataset: ~22 labeled / 37 boxes; 3 marked `duplicate`.

**⚠️ Operational gotcha — the relay is flaky for long-poll.** getUpdates through the Cloudflare worker intermittently resets/times-out → dropped button taps → Almas's uploads stall mid-flow. Mitigations: `start_polling(polling_timeout=10)` + non-fatal `_ack()` so a stale tap still saves (10 Jun); **retry middleware on outbound calls** (`bot/net.py` `RelayRetryMiddleware`, 12 Jun) — retries transient `ConnectionResetError`/aiohttp `ClientOSError` 3× w/ backoff so one dropped sendMessage no longer aborts the photo flow mid-step (real API errors pass through). Root incident that prompted it: a `ConnectionResetError` on the "type pest name" follow-up message stalled an upload (data was saved, the prompt just never sent). **Durable fix if it still recurs:** a sturdier outbound path (proxy/VPN), not the Cloudflare worker. Also: avoid rebuilding the bot while Almas is actively uploading.

**Portability kit (11 Jun):** `SETUP.md` (new-machine + workflow), `scripts/claude-memory.sh` (save/restore this memory snapshot under `docs/claude-context/`), `docs/SECRETS_LOCKBOX.md` (plan to move secrets off hand-copied `.env`).

**Open threads (pending):**
- Almas has ~6 incomplete drafts (`awaiting_metadata`) — he completes via `/finish`; `/cancel` discards. (2 complete ones were finalized 11 Jun.)
- Almas to confirm the **15-priority pest** list; note *Diuraphis noxia* (Тля ячменная = Russian wheat aphid) is in the candidate pool — maybe promote.
- **Grant docs (iCloud `Flagleaf/`): taxonomy update ON HOLD per user** until more info arrives — they should eventually say "23 сорняка, 11 болезней, **15 вредителей**, 6 стрессов". Docs already synced Whisper→Yandex SpeechKit/YandexGPT.
- **v0 bootstrap:** smoke test done 11 Jun → leans LOW end (domain gap = camera angle/lighting/framing; soil matches MFWD). v0 not worth prioritizing; **collection is the priority** (~800-frame season target; at ~22). Optional lever: a light shooting protocol for Almas.

**Infra & agent-prep (11 Jun pm):** Portability kit (`SETUP.md`, `scripts/handoff.sh|pickup.sh|deploy.sh|claude-memory.sh`, `Makefile` → `make handoff/pickup/deploy`, `docs/claude-context/` snapshot). **Tailscale** installed on prod (server `flagleaf-prod`=100.121.33.2 on the tailnet; `ssh flagleaf` / `ssh flagleaf-pub`; **public SSH still open**; live tailnet SSH from RU is intermittent → public is the reliable path; Taildrop works). Almas **shooting guideline**: `docs/photo-guide-almas-ru.md`. **Agent decision:** don't build the agent's reasoning/CV core yet (data-gated; collection is the priority) — started the neutral data layer instead: **pesticide catalog ingested** (`catalog/ingest_pesticides.py` → `pesticide_applications`, 3648 pilot-crop rows from the Минсельхоз Госкаталог opendata; re-runnable, `--all` for full). **Treatment-history pipeline built** (migration 0013 `field_treatments`; `catalog/ingest_treatments.py` CSV importer + `catalog/treatment_history_template.csv` + `docs/treatment-history-ru.md`) — self-tested, **awaiting the farm's data** (Almas/Ole export from CropWise/1C or fill the template). **Field 76/108 fully digitized from CropWise export (11 Jun pm):** `catalog/parse_cropwise.py` (local xlsx→CSV parser for CropWise 'multiprotocol' exports) → `field_treatments` 95 ops/5 seasons (42 protection/24 fert/15 tillage/14 sowing, migration 0014 added `op_category`). `catalog/ingest_weather.py` → `weather_daily` (migration 0015): 6737 Valujki (2007-26) + 2715 meteoblue (2019-26) daily rows. `catalog/ingest_vegetation.py` → `vegetation_weekly`: 1300 rows / 754 NDVI (2001-26). **All 3 pilot fields now have the full agent data layer** (76/108, 121/140, 171/99): treatments (95+101+89 = 285 ops, seasons 2021-26), weather_daily (~9.4k days/field: Valujki 2007-26 + meteoblue 2019-26), vegetation_weekly (~1300 wks/field, 754-765 NDVI), + pesticide catalog. Pipeline: export each field's CropWise `multiprotocol.xlsx` + weather/veg (csv OR xlsx) → `_incoming/` (gitignored) → `parse_cropwise.py` (local) → `ingest_treatments/ingest_weather/ingest_vegetation`. **Agent read layer (11 Jun):** `bot.db.field_card_text(field, farm_id)` = shared integrated card (ops by category + **protection rotation per season×crop** + recent treatments **with active substances** + weather + NDVI + catalog candidates), surfaced via **`/field <поле>` admin bot command** (handlers.py, in ADMIN_HELP, live) AND `catalog/field_summary.py` CLI (thin wrapper). **Active substances (д.в.) enriched** into `field_treatments` via `catalog/enrich_active_substances.py` (normalizes CropWise trade names → catalog `active_substances`; 106/129 protection rows filled; rest = adjuvants/biostimulants/typo). **Pesticide catalog now FULL** (`ingest_pesticides --all`, 14380 rows / 1488 crop variants — was pilot-only 3648). The field card also includes **MoA/resistance analysis** (`bot/moa.py`: HRAC/FRAC/IRAC map for the active substances in use; flags MoA over-reliance ≥6 apps/≥4 seasons — real finding: all 3 fields over-use **ALS-ингибиторы HRAC 2**, 8-10 apps/5 seasons; + triazoles FRAC 3, ACCase HRAC 1) and **NDVI anomaly detection** — now uses a **same-crop baseline** (`ndvi_anomaly_samecrop`: recent weeks vs the same-crop week-of-year norm, pooled across all fields/years via crop-per-(field,season); removed the crop-rotation false flags; spring wheat→"нет истории", soy/sunflower 2026→в норме). MoA map is now **catalog-wide** (~214 substances, 66% of catalog occurrences classified; rest = agrochemicals/biostimulants with no MoA; `catalog/moa_coverage.py` reports it). Agent reasoning core still waits for a CV model + more photos. Minor cosmetic: catalog crop-label uses latest treatment's crop (may show озимая vs яровая wheat; count is wheat-correct).

**Full-farm CropWise sync — DECIDED (user chose live sync, 11 Jun)** for all ~300 farm fields (a deliberate step beyond discovery-phase "build to learn"). RU reachability GREEN: `api.cropwise.com`/`open-platform`/`operations.cropwise.com` respond from the prod VM. API landscape: **Cropwise Open Platform** — Core Services (orgs→properties→fields→agro-operations=treatments, `/v2/...`) + Remote Sensing API (NDVI/vegetation indices) + AgInsights. **BLOCKED on Syngenta issuing OAuth credentials** (contact-Syngenta, no self-serve; granting a competitor API access is the real unknown). Built `catalog/cropwise_probe.py` (read-only OAuth2 client-credentials probe → discover real shapes before building) + `.env.example` CROPWISE_* vars. Plan once creds land: probe → build sync (fields→treatments, Remote Sensing→NDVI, **regional weather not ×300** since fields share the area, auto-create field rows, scheduled cron).

**Whole-farm bulk export ingested — Stage 1 done (11 Jun):** Alexey did a CropWise UI bulk export (in `_incoming/`: `#fields-*.xlsx`, `АОНЗК.json` GeoJSON 283 polygons, `vegetation_history_daily.xlsx` wide NDVI 2002-26 all fields, `weather_history_daily_company.xlsx` per-group, plant_threats empty). **KEY FINDING: the whole-farm export has NO spray-operations history** — so MoA/resistance depth stays pilot-only (the 3 fields) until treatments come via the API or per-field multiprotocol exports. Ingested **283 fields + 558 crop-year rotation rows** (migration 0016 `fields.cropwise_id/field_group` + `field_crops`; `catalog/ingest_fields.py`, openpyxl now in image; 3 pilots matched by number+area, 280 added is_pilot=false). `/field` card gained a 🌾 Севооборот section. **Whole-farm bulk export now FULLY ingested (11 Jun):** + daily NDVI (`catalog/ingest_ndvi.py`: wide xlsx → ISO-week mean → `vegetation_weekly` source='cropwise_bulk'; **209,257 rows, 283 fields, 2002-2026**; old single-field NDVI dropped to unify) + PostGIS geometries (migration 0017 `fields.geom` MultiPolygon/4326 + GIST; `catalog/ingest_geometry.py` via ST_GeomFromGeoJSON; **283/283, cropwise_id set, total farm 12,942 ha**). The **same-crop NDVI baseline is now farm-wide** — `field_card_text` reads `field_crops` rotation, so any field (incl. treatment-less ones) is checked against 100+ field-years (e.g. winter wheat = 134). `openpyxl` in image. **Only treatments remain pilot-only** (need API / per-field multiprotocol). Bulk export needs NO Syngenta reply; API still the path for live + treatments.

**Manual multiprotocol treatment pipeline — now batch + API-safe + proven on prod (12 Jun):** Hardened the per-field path so the farm's spray history can be loaded now without waiting on the Syngenta API. (1) **Idempotent/dedup** — migration 0018 adds a `NULLS NOT DISTINCT` unique index on the treatment natural key (field/date/operation/product/dose/area); `ingest_treatments` now `INSERT … ON CONFLICT DO NOTHING` and reports inserted/skipped/unmatched. `source` is stored for provenance but is NOT in the key, so a later CropWise-API sync can't double-count — no delete-by-source needed. (2) **Batch** — `parse_cropwise.py` takes a whole folder → one combined CSV; rows tagged `source='cropwise_multiprotocol'`. (3) **GOTCHA (cost us a real bug, caught by testing one file first):** the export filename's number is a CropWise/export id, NOT the field number (`2026-06-12_field_144_multiprotocol.xlsx` is really **Поле 125 · Хлевище, 44.07 ha**), and real exports lead with a **`Паспорт поля` sheet** — that sheet (Поле № + Группа полей + Площадь) is the source of truth for field identity. Parser reads it and emits the exact fields-table name `Поле <№> · <группа>` + area; ingest matches by exact name then **leading-number + area fallback** (mirrors `ingest_fields`), so partial-area ops still link; unmatched rows kept as text (field_id NULL). **WHOLE FARM treatment history now loaded to prod (12 Jun): 19,579 ops across 270 fields, 2021-2026** (protection 8235/fert 4515/tillage 4114/sowing 2715) — from 283 multiprotocol xlsx (`Flagleaf_data/CropWise field multiprotocols/`, grouped in crop subfolders; `parse_cropwise` now recurses), **unmatched=0** (every field linked via passport-name + number/area fallback), 13 fields empty (fallow/grassing/new). Idempotent re-run confirmed (skips dups). This CLOSES the treatment gap the bulk export left — no longer pilot-only. **д.в. enriched farm-wide (12 Jun):** `enrich_active_substances.py` run on prod → 6931/8235 protection rows have active substances (84%; rest = adjuvants/biostimulants/typos, no MoA — expected). So `/field` MoA/resistance analysis now works for all 270 fields, not just pilots. **Photo flow rolled out to ALL fields (12 Jun):** picker still lists 3 pilots as quick buttons, but «Другое поле (по номеру)» → type a field number (`find_fields_by_number`, matches 125 / 76/108 / 31-1; ambiguous-across-groups → pick buttons; /skip → off-pilot field_id NULL) → links to ANY of the 270 fields, so the in-bot treatment-link question now triggers farm-wide. New state `PhotoForm.field_number`; photo-save extracted to `_save_photo_for_field`. Recipe for the rest: drop all `*.xlsx` in a folder → `parse_cropwise.py <folder> out.csv` (check each file's resolved field in the log; `--field-map filename,field` to pin any that don't resolve) → scp → `run --rm -T -v <abs csv>:/data.csv bot python -m catalog.ingest_treatments /data.csv`. Whether to do all ~300 is the user's call (heavy manual UI export work; not on the photo-collection critical path) — the pipeline no longer cares about timing or the API.

**Pilot expanded to 12 fields / +3 junior agronomists (12 Jun):** Олег Костенников, Валерий Швец-Ковган, Олег Сорока — each responsible for a Соя/Озимая пшеница/Подсолнечник field (mapping in `docs/pilot-agronomists.md`; resolved the farm's «номер/площадь-га» notation e.g. 119/107 = field №119, 107 ha → `Поле 119 · Хлевище`). Migration 0020 flipped is_pilot on the 9 fields (matched by unique номер); picker now shows all 12 (founder chose: everyone sees all, responsibility organizational; field buttons now show crop). Onboarding via `/adduser <id> <имя>` (id = what the bot shows on first Start). **Team as of 13 Jun:** admins = Алексей Халиуллин (founder, 417450813) + **Алексей Ефременко (deputy director, 1889637036 — elevated to admin: DB role + added to ADMIN_TG_IDS so he gets admin alerts)**; agronomists = Алмас Касумов, Олег Сорока, Олег Костенников. **Team complete (confirmed 24 Jun):** Валерий Швец-Ковган now onboarded (tg 831602921, agronomist, active) — all 3 junior agronomists + Almas (chief) + Евгения (annotator) + 3 admins (Халиуллин/Ефременко/Дурнев) registered. All on farm_id 1 → see all 12 pilot fields. Bot **display name is now "Flagleaf"** (handle stays @nzkweed_bot — Telegram doesn't allow changing a bot's username; would need a new bot). All 12 fields have full treatment history + `/field` cards.

**/field now attaches outline maps (12 Jun):** two PNGs after the text card — close-up (field + neighbours) + farm overview (all 283 fields, target highlighted red). Rendered offline with **Pillow** from PostGIS geometries (`bot/fieldmap.py`; `db.get_field_polygons` = ST_AsGeoJSON+ST_Simplify, `db.resolve_field_id` mirrors field_card_text matching). No basemap/network → reliable from the RU VM. Founder chose outline-only (offline) over a Yandex satellite basemap; satellite remains a possible later upgrade for the close-up. Best-effort (map failure never breaks the card). Verified against real prod geometry (283 polygons → valid PNGs, looks correct). Pillow added to requirements. **Field card also now shows 🫘 Семена (current-season seed brand from latest sowing op) + 🧴 Удобрения по сезонам/последние внесения (fertilizer history)** — mirrors the chemical (protection) sections, so all three input types (защита/удобрения/семена) are covered.

**STRATEGIC PIVOT — bot to gradually REPLACE CropWise (12 Jun, founder):** while collecting photos, also replicate CropWise's most-useful functions but as a **proactive, succinct chatbot** (CropWise = pretty but passive/overloaded dashboard agronomists don't really use; the wedge is proactive Q&A that answers the agronomist's real need without overload). Starting with NDVI. **NDVI source fact:** CropWise computes NDVI from **Sentinel-2** (free ESA Copernicus, ~10 m, ~5-day); our existing NDVI is that, exported from CropWise (`vegetation_weekly` source=cropwise_bulk, static, fresh to 2026-06-08). **Reachability finding (probed from prod VM):** Copernicus direct (`dataspace.copernicus.eu`) is **blocked from RU**, but the **AWS Sentinel-2 Open Data mirror** (`earth-search.aws.element84.com` STAC + `sentinel-cogs` S3) is **reachable (200)** → Phase B pulls Sentinel-2 from AWS, independent of CropWise.
**NDVI Phase A — DONE (13 Jun):** proactive NDVI watch. `bot/db.ndvi_scan` runs the same-crop anomaly engine (`moa.ndvi_anomaly_samecrop`) over all pilot fields; `bot/ndvi_watch.py` builds a succinct digest (names ONLY fields below same-crop norm, silence/▢ when normal). **`/scan` command** (+ RU aliases осмотр/проверка, in menu+help, everyone) replies on demand; **`scripts/ndvi_watch.sh` weekly cron Mon 06:00** delivers via `labeling/alert.py` to ADMIN_TG_IDS **only when anomalies** (anti-dashboard). Live test flagged Поле 217/Подсолнечник & 39/Соя below norm. **Limitation:** delivers to admins (not per-agronomist — no `responsible_user_id` link yet + 2 juniors not onboarded); and data was static until Phase B (now done).
**NDVI Phase B — DONE (13 Jun): own Sentinel-2 → NDVI feed, CropWise-independent.** `catalog/ingest_sentinel_ndvi.py`: STAC search (earth-search, plain `requests`) → read red(B04)+nir(B08)+SCL COGs directly from `sentinel-cogs` S3 over https (windowed per field polygon via `rasterio`) → SCL cloud-mask (keep classes {4,5}) → mean NDVI → upsert `vegetation_weekly(source='sentinel')` AND delete the static `cropwise_bulk` row for that field+week (one NDVI per week, fresh-on-top; reader unchanged). **Fewest intermediaries** (no Sentinel Hub, no SDK, no key). **KEY FINDING:** earth-search/sentinel-cogs COGs store reflectance as **DN/10000 with NO +1000 baseline offset** (verified: red DN ~370; applying -1000 → negative refl, NDVI ~-0.4; no offset → 0.473, matching CropWise's 0.45). NDVI is scale-invariant so raw DN works. **rasterio added** (pip wheel bundles GDAL; needs apt `libexpat1` in Dockerfile). First full run: **275/283 fields** got fresh NDVI (week 24, 2026-06-11), 8 cloudy/edge. **Cron:** `scripts/sentinel_ndvi.sh` Mon 05:00 (fetch) → `ndvi_watch.sh` Mon 06:00 (interpret+alert). Reachability (probed): Copernicus direct blocked from RU VM, AWS mirror OK. **NDVI scan extended to ALL fields + tuned (13 Jun):** `/scan` + the Monday morning summary now cover all ~283 farm fields (not just pilots) via `moa.ndvi_anomalies_all` (batch — precomputes the per-crop baseline once, ~2.6s for 283). First all-farm run flagged ~132/283 (false positives from a hazy Sentinel pass + the CropWise→Sentinel seam). **Tuned 3 ways → dropped to 20, all genuine sustained dips:** (1) Sentinel ingest stricter clouds (scene eo:cloud_cover<60, per-field clear-pixel ≥0.60); (2) week-over-week 'drop' fires only between consecutive SAME-source readings (source plumbed through `ndvi_rows`) — kills the history→Sentinel seam drop; (3) 'below norm' requires a SUSTAINED dip (two most-recent readings both below). Shared `moa._flag_weeks` used by both `/field` and `/scan`. Digest caps flagged at 30 (+"ещё N, see /scan"). The 20 remaining are a real cluster (mostly Красное-group Soy ~0.2 vs norm 0.41, 2 weeks) worth a ground-check. NEXT for NDVI: per-agronomist alert targeting (needs responsible_user_id + juniors onboarded); the older hazy week-24 Sentinel values remain in the DB (harmless now — tuning suppresses single-week artifacts).
**In-bot OPERATION LOGGING — DONE (13 Jun): voice/text → structured, replaces CropWise daily entry.** Agronomist describes an op («опрыскал 119 Корсаром 1.5 л/га от сорняков») by voice or text → **YandexGPT parses** it (`bot/parse_op.py` → {field,category,operation,product,dose,area_ha,target,date}) → resolve field (`db.resolve_field`) + **auto-fill д.в. from Госкаталог** (`db.lookup_active_substance`, e.g. Корсар→Бентазон) → one-line confirm → ✓ saves to `field_treatments` (source='bot', idempotent via natural-key index; `db.insert_bot_treatment`). **`/log`** command (+ RU aliases запись/журнал) + `OpLogForm` (awaiting→confirm). **Daily nudge** `bot/op_nudge.py`/`scripts/op_nudge.sh` cron **15:00 UTC (18:00 MSK)** → inline «➕ Записать обработку» button (callback oplog:start) drops the agronomist into the flow; sent to all active users (`db.get_active_users`). Verified end-to-end on prod (parse+resolve+дв+insert). **Caveat:** only pays off if the bot REPLACES CropWise entry (no double-logging); nudge currently to all active users (per-agronomist targeting later). **All fields open to all agronomists (13 Jun):** no per-agronomist/field gate exists — `/log` and `/field` resolve ANY of the 283 fields farm-wide (resolve_field, not pilot-scoped); photos reach any field via «по номеру». (Did NOT flip all 283 to is_pilot — would flood the photo quick-button picker; only photo quick-buttons + /scan stay pilot-scoped.) **Duplicate-conflict resolution for shared logging:** exact dup auto-skipped (natural-key ON CONFLICT, `insert_bot_treatment` returns inserted? → "дубликат пропущен"); near-dup (same field+date+category+product, e.g. a colleague already logged it, possibly different wording) → log-time WARNING in the confirm naming who logged it, agronomist taps Отмена to skip (`db.find_similar_treatment`). Verified on prod. The genuinely-daily scouting is already captured by the photo flow; this fills the operations-log gap. Scouting/spreadsheet-bulk paths remain: photo flow + `ingest_treatments` CSV importer. **Export loop closed (13 Jun):** `catalog/export_multiprotocol.py` rebuilds a CropWise-style multiprotocol **.xlsx** from `field_treatments` (Паспорт поля sheet + per-season sheets split tillage/sowing/fertilizer/protection, incl. д.в. inline — the inverse of `parse_cropwise`). **`/export <поле>`** bot command (+ RU aliases экспорт/выгрузка) sends it as a Telegram document (builder lazy-imports openpyxl); also a CLI. Verified on field 119 (7 sheets, 16 KB). So ops logged in the bot can come back out in the format the farm knows. **Cron now:** backup 03:00, labeling 03:30, sentinel-NDVI Mon 05:00, ndvi-watch Mon 06:00, op-nudge 15:00 daily.

**Infra hardening (13 Jun) — backup outage caught & fixed + productionization plan.** Verifying a restore caught that **nightly DB backups had silently failed for ~2 weeks** (last good 2026-05-29): `backup.sh` was **100644 in git**, so every `rsync` deploy overwrote the server copy without +x → cron "Permission denied". **LESSON: any cron/deploy script MUST be git-mode 100755 or rsync deploys strip the exec bit** (all others were already 755; backup.sh was the lone victim). Fixed: `git update-index --chmod=+x backup.sh`, ran a fresh backup (6.6 MB, restores cleanly — 19.6k treatments/209k NDVI/286 fields verified into a temp DB), and added an **ERR-trap Telegram alert** to backup.sh so a silent backup failure can't recur. **Decision: do NOT migrate to product infra during the pilot** (premature; ~5-10× cost; slows feature iteration; against "build to learn"). Wrote `docs/productionization-roadmap.md` (pilot→product changes, explicit migration TRIGGERS = 2nd customer / felt outage / 2nd dev; order: managed PG + CI/CD first; stay on Yandex/RU). **Lockbox secrets:** `deploy/fetch-secrets.sh` implemented (Pattern A, refreshes only secret keys in .env); remaining = 3 YC-console actions only the founder can do (create secret, grant SA lockbox.payloadViewer, attach SA to VM) — see `docs/SECRETS_LOCKBOX.md`.

**NDVI morning summary extended to ALL fields + false-positive tuning (13 Jun):** `ndvi_scan` now covers all ~283 farm fields (not just pilots), using a batch baseline (`moa.ndvi_anomalies_all`, precompute per-crop week baseline once → ~2.6s). First all-farm run flagged ~132/283 — artifacts of a hazy Sentinel pass + the CropWise-history→Sentinel seam. Three fixes cut it to **20/283** (all genuine sustained dips): (1) Sentinel ingest stricter clouds (scene `eo:cloud_cover`<60, per-field clear-pixel ≥0.60); (2) week-over-week 'drop' rule fires only between consecutive SAME-source readings (source plumbed through `ndvi_rows`) — kills the seam; (3) 'below norm' requires a SUSTAINED dip (two most-recent readings both below). Shared `moa._flag_weeks` used by both `/field` and `/scan` for consistency. Digest caps the flagged list at 30 with a "+N more" overflow.
**Phenology (days-after-sowing) baseline — DONE (13 Jun), replaces week-of-year.** Each NDVI reading is bucketed by **days-after-sowing//7** and compared to same-crop peers at the SAME phase, so later-sown fields aren't flagged for being young (this reclassified the whole flagged Красное soy block as normal-for-phase → the earlier 20 flags dropped to 0 this week — conservative + correct: those were late-sown, and single fresh-Sentinel-week dips don't flag without a sustained 2nd week). `db._crop_sow_maps` builds crop+sow_date per (field,year) from field_treatments sowing-op dates (2021-26) overlaid by field_crops (2025-27, authoritative incl. winter crops' autumn sow); `db._norm_crop` canonicalizes wheat labels (winter vs spring kept separate). Baseline pool grew (e.g. Соя 235→**541 полей-лет**). The soy phase-curve is textbook (0.36 at sowing → peak 0.72 at ~8 wk → 0.30 senescence), confirming validity. Windows in days: below-norm 14–300d (covers winter wheat spring), sharp-drop 14–110d (active growth). Fields without a known sow date are skipped (no false flag). Card/scan note now reads "база: «<crop>» по фазе".

**CropWise API access OBTAINED (15 Jun) — via Operations, not Open Platform.** CropWise issued access through the **operations.cropwise.com "linked devices"** flow → a personal token used as header **`X-User-Api-Token`** on the **Cropwise Operations API v3** (`https://operations.cropwise.com/api/v3`). Stored as `CROPWISE_OPERATIONS_TOKEN` in the prod `.env` (move to Lockbox when finalizing). The API exposes the full operations model: `agro_operations` (treatments; nested `application_mix_items[]` = products+rates, applicable_type Chemical/Fertilizer/Seed → `chemicals`/`fertilizers`/`seeds` dicts), `fields`, `field_groups`, `work_types`, `crops`, `seasons`. ID-based pagination (`from_id`, per_page≤1000). **Sync BUILT + dry-run-validated** (`catalog/cropwise_ops_sync.py`, `--dry`/`--field`): maps each op → field_treatments rows (one per mix item; cat from applicable_type/work_type), idempotent upsert source='cropwise_api', retires manual rows only for covered fields. Dry-run: 8,872 rows from 14,971 ops, clean mapping, 99/100 API fields matched our DB. **NOT RUN — HELD (founder, 15 Jun) pending CropWise.** **COVERAGE GAP:** the token sees only **100 fields** (95 Красное + 5 Хлевище + empty Тишанка) vs our **283** — almost all of Хлевище is missing, incl. pilots 119/144/170/268 and the originals 76/108, 121/140, 171/99 (only Красное pilots 10/32/217 present). Token looks scoped to one org/sub-account. **Action: founder to ask CropWise for API access covering ALL АО НЗК fields**; then run the sync (replaces the manual multiprotocol import, weekly cron) — until then manual data stands.
**RESOLVED (24 Jun): coverage gap CLOSED — expanded token now sees all 443 АО НЗК fields (was 100).** Sync is LIVE and the source of truth: `field_treatments` = **25,076 rows, 100% source='cropwise_api', 272 fields, fresh to 2026-06-22**; manual multiprotocol import fully retired/replaced; **weekly cron `scripts/cropwise-sync.sh` Mon 04:00**. 130 protection products enriched with д.в. 79 API fields unmatched (archived/old-season, not in our `fields` table) = optional backfill via `ingest_fields`. So `/field` MoA/resistance + `/plan` now run on live API data farm-wide.

**OPEN ITEMS — VERIFIED against prod/repo (24 Jun):**
- ✅ DONE (verified, off the list): CropWise API sync (25,076 rows source=cropwise_api, 443 fields visible, weekly cron Mon 04:00) · Валерий Швец-Ковган onboarded (team complete) · Поле 39 pricing+baseline (4 prices set, ₽ baseline 895 602, /plan verified) · **Lockbox appears DONE** (git "secrets: cut over to Yandex Lockbox" + SETUP uses `deploy/fetch-secrets.sh` as standard; live secret-read check was blocked by the safety classifier as credential-probing → confirm via `yc lockbox payload get --id e6qavh2hnlj0fr9b73sh --format json | jq '.entries|length'`=7).
- ⏳ STILL OPEN: **(Almas, highest value)** ЭПВ thresholds — no data store, `/plan` reasons about ЭПВ only generically (4 prompt mentions); needs Almas's numbers encoded as grounding. · **(Almas+field)** finish Поле 39 savings loop — `scouting_of_field39=0`, plan_runs have no `outcome` → needs a real scouting pass + Almas's 1-vs-2-pass call + `/savings`. · **(tech, mine)** NDVI per-agronomist targeting — no `fields.responsible_user_id` column, only 1 push_subscription; alerts still admin-only. · **(tech, mine, optional)** backfill the ~79 archived CropWise fields — `fields`=286, API sees 443. · **(founder)** drone flight (next Pilot-v2 sensing proof). · **(backlog)** Max/МАКС port — `docs/max-port-plan.md` only, no code.
- 🗑 MOOT/RESOLVED: grant-docs taxonomy update (no longer tracked in memory; Pilot-v2 pivot superseded the photo-volume/grant framing) · flagleaf.ru topology (the 158.160.46.89 references are a stale IP; real VM = 111.88.248.159 — effectively one VM).

Serves [[newgrain-goal-and-principle]].

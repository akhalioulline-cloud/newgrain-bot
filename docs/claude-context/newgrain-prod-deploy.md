---
name: newgrain-prod-deploy
description: "NewGrain production deployment — Yandex Cloud VM, how to reach it, deploy & backups"
metadata: 
  node_type: memory
  type: project
  originSessionId: 00400038-017e-4a92-be90-dfab003d97b1
---

NewGrain runs in production on a **Yandex Cloud VM** (cloud `newgrain-cloud`, folder `default`).

- **SSH:** `ssh newgrain@111.88.248.159` (key `~/.ssh/id_ed25519`; key-only auth). VM = Ubuntu 22.04, 2 vCPU / 4 GB, zone `ru-central1-a`, sg `newgrain-sg` (inbound TCP 22 only). The public IP `111.88.248.159` was **reserved as static** (13 Jun, via "Make static" in the console) — it no longer changes on stop/start. (Was ephemeral and changed 158.160.46.89 → 111.88.248.159 once before reserving.) (Tailscale `flagleaf`/100.121.33.2 is IP-stable but RU tailnet SSH is intermittent → public IP stays the deploy path.)
- **Stack:** `docker compose -f docker-compose.prod.yml` in `/home/newgrain/newgrain-bot` — only `postgres + redis + bot` (no MinIO/api/worker), no exposed ports, `restart: unless-stopped`. Code baked into image (no bind-mount) → rebuild to ship changes.
- **Storage:** Yandex Object Storage bucket `newgrain-data-prod`, endpoint `https://storage.yandexcloud.net`, region `ru-central1`. Service account `newgrain-bot` (role `storage.editor`) static key. Bucket is private.
- **Secrets:** now in **Yandex Lockbox** (secret `flagleaf-prod`, id `e6qavh2hnlj0fr9b73sh`; 7 entries: BOT_TOKEN, POSTGRES_PASSWORD, DATABASE_URL, S3_ACCESS_KEY, S3_SECRET_KEY, CVAT_API_TOKEN, YC_API_KEY). The VM has service account **`newgrain-bot` attached** (granted `lockbox.payloadViewer` on the secret) → `deploy/fetch-secrets.sh` gets an IAM token from instance metadata + reads the Lockbox payload REST API (curl+jq, no yc/key file) and refreshes those keys in `.env`; wired into `deploy.sh` (best-effort). Non-secret config stays in `.env`. Rotate = edit the Lockbox entry → re-deploy. `.env.bak` is the break-glass copy. **Laptops also pull from Lockbox** (13 Jun): `fetch-secrets.sh` auto-detects — VM=metadata, laptop=`yc` CLI fallback (one-time `yc init` OAuth; founder is cloud owner so already has access). New machine = `git clone` + `cp .env.example .env` + `yc init` + `./deploy/fetch-secrets.sh`; only the SSH key is still hand-carried (see SETUP.md). Same `BOT_TOKEN` as dev → **only one bot may poll at a time**; local dev bot stopped.
- **Backups:** `backup.sh` (cron 03:00 daily) → `pg_dump | gzip` to `/home/newgrain/backups` (7-day rotation) + uploaded to `backups/` in the Yandex bucket **+ to the offsite AWS archive** (step 3, env-guarded on `OFFSITE_S3_*`). Log: `/home/newgrain/backups/backup.log`.
- **Cron jobs on the VM** (all owner-installed; Claude is hard-blocked from scheduling prod-writing/egress jobs): `03:00` backup.sh; `03:30` offsite photo/voice mirror (`catalog.mirror_offsite` → AWS, see [[newgrain-continuity-portability]]); `Mon 04:00` CropWise weekly sync+enrich (`scripts/cropwise-sync.sh`, log `cropwise-sync.log`); plus the labeling `03:30` pipeline. Source of truth = `crontab -l` on the VM.
- **CropWise sync LIVE (15 Jun 2026):** `catalog/cropwise_ops_sync.py` (Operations API v3, token in `.env` as `CROPWISE_OPERATIONS_TOKEN`) loaded the full farm — `field_treatments` is now entirely `source='cropwise_api'` (21,741 rows / 272 fields), manual multiprotocol import retired. The earlier "only 100 fields" was a **pagination bug**, not an API limit: the `fields`/`chemicals`/`fertilizers` endpoints cap `per_page` at ~100, and `_all()` stopped on the first short page — fixed to page-until-empty. `catalog/enrich_active_substances.py` fills `active_substance` (128/171 products matched). Refreshed weekly by the Mon-04:00 cron.
- **«Задания машин» report-paste → CropWise (LIVE 17 Jun 2026, IN FIELD TESTING):**
`catalog/cropwise_report.py` + bot paste-flow. Operator (Евгения) pastes a Max field
report → LLM parse → resolve work_type (217 agri types, fuzzy by name), machine (/machines
by registration_number), driver (/users by surname), products (catalog), fields → preview
card (CAReport.confirm) → ✅ creates ONE completed agro-operation PER FIELD (tank mix via
application_mix_items + work_type + driver as responsible_user_ids). Machine is NOT attached
(no machine field on agro_operations; it lives on the GPS machine_task). Operator-gated
(admin/chief_agronomist/annotator). Format + CropWise model fully decoded in
`docs/cropwise-machine-task-reports.md`. NOT yet write-tested (Claude blocked from CW writes
— Евгения tests one real report; watch: does responsible_user_ids take, work_type exactness).
Reuses `cropwise_push` create primitives.

**Bot→CropWise PUSH (LIVE 17 Jun 2026):** `catalog/cropwise_push.py` creates COMPLETED agro_operations from bot-logged ops; wired into `/log` (`handlers.on_oplog_save` → `push_treatment` off the event loop; CropWise failure = warning, never blocks the local save). Confirmed end-to-end (op 20521 done + внесение Корсар fact). THREE steps (op & внесение are separate CropWise resources): POST `/agro_operations` (planned) → POST `/application_mix_items` (the внесение, with `agro_operation_id`+fact_rate/fact_amount) → PUT status=done. GOTCHAS solved: the `strict_ami_done_status` block was a CropWise SETTING («запрет на закрытие без внесений», owner unticked it, NOT an API limit); `completed_datetime` must be ≤ now (use now-15m, not noon); embedded mix in the op POST is dropped (must POST внесение separately). In-RU model `qwen3.6` aside, this uses the Operations API. Dedup: pull sync (`cropwise_ops_sync`) skips ops whose `idempotency_key` starts with `flagleaf-` (our own pushes). Decisions: confirm-before-push (the /log ✓ IS the confirm); unmatched product → still create op, flag «впишите вручную». Catalog cached 1h in the bot. **Slot-filling (OpLogForm.filling):** if a logged op misses a required slot the bot asks one at a time (field/product/dose/crop, text or voice); field resolves across ALL 283 fields and asks «какое отделение?» when a number repeats across отделения. Validated 17 Jun (`catalog/oplog_audit.py` + `oplog_parse_test.py`): 100% product+work-type mapping over full history, parser 29/30 on realistic phrasings, 4/4 dose-clarification. **STATUS: in field testing** — awaiting bugs from agronomists + the CropWise operator before calling it done. Details in `docs/cropwise-push-status.md`.
- **Deploy a change:** `scripts/deploy.sh` (or manually: rsync repo to `newgrain@111.88.248.159:/home/newgrain/newgrain-bot/`, exclude `.git .env __pycache__ .claude`; then `./deploy/fetch-secrets.sh` to refresh secrets from Lockbox; then **`docker compose -f docker-compose.prod.yml up -d --build bot`**) (the `--build` is mandatory — code is baked into the image, NOT bind-mounted; `restart bot` alone keeps running the old image). DB migrations via `docker compose -f docker-compose.prod.yml run --rm bot alembic upgrade head` *after* the rebuild (so the migration script is in the image).

- **Telegram-block incident (started 2026-06-03):** Roskomnadzor began IP-blocking most of Telegram's API ranges from RU networks. Bot couldn't reach api.telegram.org → getUpdates timed out for ~5 days; Almas couldn't upload, no commands worked. General egress (google/yandex) stayed fine; block is **IP-based, not DPI/SNI** (curl --resolve to a non-blocked Telegram IP returns valid HTTP). The founder's out-of-RU VPNs (Hetzner 46.62.138.179, a Google Cloud VPN) are unreachable from the RU VM / disrupted from RU — RKN flags single VPS IPs. **Cloudflare's edge IS reachable from the RU VM** (cloudflare.com → 200) and is impractical to block wholesale → chosen relay path.
  - **Stopgap (live):** `docker-compose.prod.yml` bot service has `extra_hosts: ["api.telegram.org:149.154.167.220"]` pinning a currently-reachable Telegram IP. Fragile — if RKN blocks that IP, rotate to another working Telegram IPv4 (test with `curl --resolve api.telegram.org:443:<ip> https://api.telegram.org/`).
  - **Durable fix (LIVE since 2026-06-08):** Telegram traffic routes through a **Cloudflare Worker** relay. Worker URL `https://flagleaf-tg.akhalioulline.workers.dev` (founder's CF account, free tier; `ALLOWED_TOKEN` secret set = rejects non-our-token paths with 403). Server `.env` has `TELEGRAM_API_BASE=https://flagleaf-tg.akhalioulline.workers.dev`; `bot/main.py` builds Bot with `AiohttpSession(api=TelegramAPIServer.from_base(...))` when that env is set. Worker code = `deploy/telegram-relay-worker.js` (pass-through proxy to api.telegram.org); setup steps = `deploy/TELEGRAM_RELAY.md`. Verified end-to-end from the RU VM: getMe via worker → bot JSON (0.16s, no throttling); 50s of long-polling → 0 errors. **Crucial network fact:** the RU Yandex *datacenter* CAN reach `*.workers.dev` (fast) even though Russian *consumer ISPs* throttle/block Cloudflare — different paths; the bot uses the datacenter path. To deploy/manage the Worker the founder needs dash.cloudflare.com via VPN (one-time); runtime needs no VPN.
  - **extra_hosts IP-pin still present in compose** as a fallback: while `TELEGRAM_API_BASE` is set the bot ignores it (connects to the worker, not api.telegram.org). To revert to the IP-pin path: unset `TELEGRAM_API_BASE` in `.env` + `up -d bot`. Remove the extra_hosts line in a later cleanup once the relay has proven durable over weeks.
  - **Key teaching point:** the user's personal VPN cannot help the bot — the bot reaches Telegram from the RU server independently of the user's connection.

Builds on [[newgrain-status-2026-05]].
